project('cli',
        'cpp',
        version: '2.0.2',
        license: 'BSL-1.0')

includes = include_directories(['include'])
# install_headers(, subdir: 'cli')

danielecli_dep = declare_dependency(include_directories: includes)
